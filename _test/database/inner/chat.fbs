namespace ezMsg.Database.Chat;

table Chat {
    UID:ulong;
    MembersUIDs:[ulong]; // List of User IDs who are members of the chat
    MessagesUIDs:[ulong]; // List of Message IDs in the chat
    AttachmentsUIDs:[ulong]; // List of File IDs attached to the chat
}
